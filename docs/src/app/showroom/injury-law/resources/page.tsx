import Link from "next/link";
import { InjuryPageIntro } from "@/features/demos/injury-law/injury-layout";
import { injuryBase, resources } from "@/features/demos/injury-law/data";
export const metadata={title:"Resources"};
export default function Page(){return <main id="injury-main"><InjuryPageIntro eyebrow="Resources · General information" title="A library organized around the questions people actually have."><p>Every article is clearly scoped. No fictional results, testimonials, awards, rankings, or dollar outcomes are used as proof.</p></InjuryPageIntro><section className="il-section"><div className="il-shell"><div className="il-resource-grid">{resources.map(resource=><article className="il-resource-card" key={resource.slug}><small>{resource.category} · {resource.readTime}</small><h3>{resource.title}</h3><p>{resource.summary}</p><Link href={`${injuryBase}/resources/${resource.slug}`}>Read guide</Link></article>)}</div><div className="il-content-grid" style={{marginTop:"6rem"}}><aside className="il-annotation"><strong>Proof standard</strong>A premium system earns trust through useful process, transparent limitations, verified people, and accurate content—not fabricated outcomes.</aside><article className="il-prose"><h2>Why there are no case results in this demonstration</h2><p>Results must be real, permissioned, accurately described, contextually complete, and reviewed for applicable advertising rules. This fictional demonstration has none of that source material, so it makes no outcome claims.</p></article></div></div></section></main>}
