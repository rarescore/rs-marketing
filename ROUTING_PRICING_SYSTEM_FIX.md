# Routing, Pricing and Connected-System Fix

## Fixed
- Top navigation now uses dedicated page routes for Contact, Audit, Process, Reviews and Pricing.
- Active page is visually indicated in the header.
- Pricing remains the original expanding horizontal-plan concept, but the interaction was rebuilt to be deterministic on click/tap/keyboard.
- Pricing supports direct hashes such as `/pricing#growth` and `/pricing#website`.
- Mobile pricing becomes a stable vertical tap accordion.
- Rebuilt the “Nothing works alone” chapter as a scroll-drawn connected signal map.
- The new system animation uses one SVG path and one signal point instead of multiple continuously animated floating objects.
- The active growth stage changes as the user scrolls: Website → Search → Content → Paid Growth → Automation.
- Added a direct link from the system chapter to the dedicated Process page.

## Preserved
- Pomegranate animation code/assets were not changed in this pass.
- Existing plan names and prices remain unchanged.
