import Link from "next/link"; import { InjuryPageIntro } from "@/features/demos/injury-law/injury-layout"; import { resources } from "@/features/demos/injury-law/data";
export const metadata={title:"Resources",robots:{index:false,follow:true}}; export default function Page(){return <main id="injury-main"><InjuryPageIntro eyebrow="Attorney review pending" title="Practical answers after an accident."><p>These original article drafts are provided for review and will remain noindex until an attorney name and review date are supplied.</p></InjuryPageIntro><section className="il-section"><div className="il-shell il-resource-grid">{resources.map(r=><article className="il-resource-card" key={r.slug}><small>{r.category} · {r.readTime}</small><h2>{r.title}</h2><p>{r.summary}</p><Link href={`/resources/${r.slug}`}>Read draft →</Link></article>)}</div></section></main>}
