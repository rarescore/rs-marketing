import type { Metadata } from "next";
import Link from "next/link";
import { PlumbingCta, PlumbingPageIntro } from "@/features/demos/plumbing/plumbing-layout";
import { plumbingBase } from "@/features/demos/plumbing/data";
export const metadata: Metadata = { title: "Home plumbing resources" };
const guides = [["Find and document your main shutoff", "A safe, owner-held record for a known and accessible shutoff."], ["What static and residual pressure mean", "How to record measurements without turning one reading into a diagnosis."], ["Prepare for a water-heater conversation", "Demand, recovery, location, fuel, access, and manufacturer information."], ["Build better sewer evidence", "Recurrence, affected fixtures, cleanout access, prior work, and camera documentation."]];
export default function ResourcesPage() { return <main className="pl-main pl-inner" id="plumbing-main"><PlumbingPageIntro code="Resource library / homeowner field notes" title="Know what to record—and what not to assume."><p>Clear, calm preparation guidance supports a better professional conversation. It does not replace qualified evaluation.</p></PlumbingPageIntro><section className="pl-resource-list">{guides.map(([title, copy], index) => <article key={title}><span>{String(index + 1).padStart(2, "0")}</span><div><h2>{title}</h2><p>{copy}</p></div><Link href={`${plumbingBase}/tools`}>Use the related worksheet →</Link></article>)}</section><PlumbingCta title="Create one record for the whole home." copy="The Plumbing Passport brings equipment, materials, shutoffs, observations, and review questions together in a downloadable owner-held brief." href={`${plumbingBase}/tools/plumbing-passport`} label="Build the Passport" /></main>; }
