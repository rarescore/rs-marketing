export default function ShowroomLoading() {
  return (
    <main className="demo-shell" aria-busy="true" aria-label="Opening industry demo">
      <div className="demo-shell__gateway">
        <p className="demo-shell__eyebrow">Opening the selected world…</p>
      </div>
    </main>
  );
}
