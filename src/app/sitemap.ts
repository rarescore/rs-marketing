import type { MetadataRoute } from "next"; import { practiceAreas, resources } from "@/features/demos/injury-law/data"; export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://example.com";return ["","/practice-areas","/process","/after-an-accident","/resources","/consultation","/privacy","/terms","/advertising","/accessibility",...practiceAreas.map(a=>`/practice-areas/${a.slug}`),...resources.map(r=>`/resources/${r.slug}`)].map(url=>({url:base+url,lastModified:new Date()}));}
