import type { MetadataRoute } from "next"; import { practiceAreas } from "@/features/demos/injury-law/data"; export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://example.com";return ["","/practice-areas","/process","/after-an-accident","/consultation","/privacy","/terms","/advertising","/accessibility",...practiceAreas.map(a=>`/practice-areas/${a.slug}`)].map(url=>({url:base+url,lastModified:new Date()}));}
