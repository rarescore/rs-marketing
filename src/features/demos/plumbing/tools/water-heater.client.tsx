"use client";
import { useState } from "react";
import { waterHeater, type ToolResult } from "../tool-engines";
import { ToolFrame, Result, downloadRecord } from "./tool-shared";
import { useSystemLens } from "@/features/system-lens/system-lens-store";
export default function WaterHeaterTool() { const [result, setResult] = useState<ToolResult | null>(null); const record = useSystemLens((state) => state.record); return <ToolFrame eyebrow="Tool 03 / Demand worksheet" title="Water Heater Demand + Recovery Planner" intro="Compare one peak-use scenario with an indicative tank recovery band. Every assumption remains visible."><form className="pl-tool-form pl-instrument-form" onSubmit={(event) => { event.preventDefault(); const d = new FormData(event.currentTarget); const next = waterHeater({ people: Number(d.get("people")), showers: Number(d.get("showers")), minutes: Number(d.get("minutes")), gpm: Number(d.get("gpm")), rise: Number(d.get("rise")), tank: Number(d.get("tank")), input: Number(d.get("input")), efficiency: Number(d.get("efficiency")) / 100 }); setResult(next); record({ scope: "plumbing", type: "tool_completed", signal: "Water-heater planning completed", response: "Demand assumptions and recovery band prepared", value: "More qualified equipment conversation", status: "derived" }); }}><div className="pl-form-grid"><label>Household size<input name="people" type="number" min="1" max="20" defaultValue="4" required /><span>people</span></label><label>Back-to-back showers<input name="showers" type="number" min="0" max="12" defaultValue="3" required /><span>showers</span></label><label>Average shower length<input name="minutes" type="number" min="1" max="40" defaultValue="10" required /><span>minutes</span></label><label>Shower flow<input name="gpm" type="number" min="0.5" max="6" step="0.1" defaultValue="2" required /><span>GPM</span></label><label>Temperature rise<input name="rise" type="number" min="30" max="100" defaultValue="70" required /><span>°F</span></label><label>Tank nominal volume<input name="tank" type="number" min="10" max="120" defaultValue="50" required /><span>gallons</span></label><label>Burner input<input name="input" type="number" min="5" max="200" defaultValue="40" required /><span>kBTU/hr</span></label><label>Assumed efficiency<input name="efficiency" type="number" min="50" max="100" defaultValue="62" required /><span>%</span></label></div><button className="pl-button pl-button--primary" type="submit">Calculate preliminary band</button></form>{result && <Result result={result} onDownload={() => downloadRecord("field-standard-water-heater-plan.txt", result.metrics.map((m) => `${m.label}: ${m.value}`).join("\n"))} />}</ToolFrame>; }
