import{useLayoutEffect,useMemo,useRef}from'react';import{gsap}from'gsap';import{ScrollTrigger}from'gsap/ScrollTrigger';gsap.registerPlugin(ScrollTrigger);export default function ScrollReveal({children,className='',dark=false}){const ref=useRef(null);const words=useMemo(()=>String(children).split(/(\s+)/).map((w,i)=>/^\s+$/.test(w)?w:<span className="reveal-word" key={i}>{w}</span>),[children]);useLayoutEffect(()=>{const ctx=gsap.context(()=>{gsap.fromTo(ref.current.querySelectorAll('.reveal-word'),{opacity:.12,y:18,filter:'blur(5px)'},{opacity:1,y:0,filter:'blur(0px)',stagger:.035,ease:'none',scrollTrigger:{trigger:ref.current,start:'top 88%',end:'bottom 58%',scrub:true}})},ref);return()=>ctx.revert()},[]);return <h2 ref={ref} className={`scroll-reveal ${dark?'dark':''} ${className}`}>{words}</h2>}