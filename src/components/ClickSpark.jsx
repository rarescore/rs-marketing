import{useRef,useEffect,useCallback}from'react';export default function ClickSpark({sparkColor='#ed111c',sparkSize=10,sparkRadius=18,sparkCount=8,duration=420,children}){const canvasRef=useRef(null),sparks=useRef([]);useEffect(()=>{const c=canvasRef.current,p=c.parentElement;const size=()=>{const r=p.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);c.width=r.width*d;c.height=r.height*d;c.style.width=r.width+'px';c.style.height=r.height+'px';c.getContext('2d').setTransform(d,0,0,d,0,0)};size();const ro=new ResizeObserver(size);ro.observe(p);return()=>ro.disconnect()},[]);const click=useCallback(e=>{const r=canvasRef.current.getBoundingClientRect(),now=performance.now();for(let i=0;i<sparkCount;i++)sparks.current.push({x:e.clientX-r.left,y:e.clientY-r.top,a:i*Math.PI*2/sparkCount,t:now})},[sparkCount]);useEffect(()=>{const c=canvasRef.current,ctx=c.getContext('2d');let id;const loop=now=>{const d=Math.min(devicePixelRatio||1,2);ctx.setTransform(d,0,0,d,0,0);ctx.clearRect(0,0,c.width/d,c.height/d);sparks.current=sparks.current.filter(s=>{const p=(now-s.t)/duration;if(p>=1)return false;const e=p*(2-p),dist=e*sparkRadius,len=sparkSize*(1-e);ctx.strokeStyle=sparkColor;ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(s.x+dist*Math.cos(s.a),s.y+dist*Math.sin(s.a));ctx.lineTo(s.x+(dist+len)*Math.cos(s.a),s.y+(dist+len)*Math.sin(s.a));ctx.stroke();return true});id=requestAnimationFrame(loop)};id=requestAnimationFrame(loop);return()=>cancelAnimationFrame(id)},[duration,sparkColor,sparkRadius,sparkSize]);return <div className="spark-wrap" onClick={click}><canvas ref={canvasRef}/>{children}</div>}