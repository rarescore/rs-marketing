import{useState}from'react';export default function AccordionGallery({items,defaultIndex=2}){const[active,setActive]=useState(defaultIndex);return <div className="accordion-gallery">{items.map((item,i)=><article key={item.label} className={`plan-panel ${i===active?'active':''}`} onMouseEnter={()=>setActive(i)} onFocus={()=>setActive(i)} tabIndex="0"><div className="plan-number">0{i+1}</div>{item.recommended&&<span className="recommended">Recommended</span>}<div className="plan-copy"><p>{item.label}</p><strong>{item.price}</strong><small>{item.period}</small><div className="purchase-count">{item.count} selected last month <em>demo</em></div><ul>{item.features.map(f=><li key={f}>{f}</li>)}</ul><a href="#contact">Choose plan</a></div></article>)}</div>}