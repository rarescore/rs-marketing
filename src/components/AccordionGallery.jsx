import{useState}from'react';
export default function AccordionGallery({items,defaultIndex=2}){const[active,setActive]=useState(defaultIndex);return <div className="accordion-gallery">{items.map((item,i)=><article key={item.label} className={`plan-panel ${i===active?'active':''}`} onMouseEnter={()=>setActive(i)} onFocus={()=>setActive(i)} tabIndex="0"><div className="plan-number">0{i+1}</div>{item.recommended&&<span className="recommended">Recommended</span>}<div className="plan-copy">{item.eyebrow&&<span className="plan-eyebrow">{item.eyebrow}</span>}<p>{item.label}</p><strong>{item.price}</strong><small>{item.period}</small><ul>{item.features.map(f=><li key={f}>{f}</li>)}</ul><a href={item.target}>{item.cta}</a></div></article>)}</div>}
